# tech.sex

Static landing page for [tech.sex](https://tech.sex). Vite + React.

## Develop

```bash
npm install
npm run dev
```

## Deploy

Push to `main`. GitHub Actions builds and deploys to GitHub Pages.
DNS lives in the `web` repo (`infra/terraform/main.tf`).
